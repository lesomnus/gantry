# The self-hosted E2E host: the machine (a developer's box or a self-hosted CI
# runner) that has docker and containerd. Everything runs locally — no vault, no
# cloud, no SSH.
[e2e]
localhost ansible_connection=local
